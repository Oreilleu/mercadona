import { Container } from "react-bootstrap";

export default function Header() {
    return <Container className="container-header">
        <p>Mercadona</p>
        <button>Admin</button>
         <p>Promotion</p>
    </Container>
}
